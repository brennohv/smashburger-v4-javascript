{
  "builds": [
    { "src": "src/api/**", "use": "@vercel/node" },
    { "src": "public/**", "use": "@vercel/static" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "src/api/$1" },
    { "src": "/(.*)", "dest": "src/api/$1" }
  ]
}
